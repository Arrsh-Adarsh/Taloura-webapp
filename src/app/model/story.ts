export interface Story {
    id: number,
    title: string,
    date: Date,
    content: string,
    author: string,
    // genere: string[]
    // summary?: string,
    // tags?: string[],
    // coverImageUrl?: string,
    // status?: 'draft' | 'published' | 'archived',
    // likes?: number,
    // commentsCount?: number, 
    // language?: string,
    // wordCount?: number,
    // rating?: number,
    // isFeatured?: boolean
};
